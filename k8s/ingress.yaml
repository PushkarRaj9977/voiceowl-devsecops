apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voiceowl-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: voiceowl-service
            port:
              number: 80
